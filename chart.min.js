/* 本文件为占位用的本地 Chart.js 欢迎文件。
   为了遵守体积限制，本仓库不包含完整的 Chart.js 源码。
   使用方法：
   1) 若你希望页面支持离线图表，请从 https://www.chartjs.org/ 下载 Chart.js 的压缩版，并把文件重命名为 vendor/chart.min.js 放入此目录。
   2) 页面会优先加载此本地文件；若本地文件为占位（本文件），图表会尝试运行但可能不会展示真实图形。

   临时的最小 stub：避免因缺失 Chart.js 导致脚本报错。
*/
(function(window){
  if(window.Chart) return; // 若已存在则不覆盖
  console.warn('本地 vendor/chart.min.js 为占位文件。若需要完整图表，请下载 Chart.js 并替换此文件。');
  // 最小 stub，仅提供构造函数接口以避免调用时报错，但不会绘制真实图表
  function noop(){ }
  function Chart(ctx, cfg){
    this.ctx = ctx; this.config = cfg; console.warn('Chart stub: 图表不会被真实绘制，请安装完整 Chart.js');
  }
  Chart.prototype.destroy = noop;
  // 标记为 stub 以便页面逻辑回退使用内置绘图
  Chart._isStub = true;
  window.Chart = Chart;
})(window);
